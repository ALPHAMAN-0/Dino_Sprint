# build outputs (sources live in src/ subfolders, so match any depth)
src/*.o
src/*.d
src/**/*.o
src/**/*.d
/dino_sprint

# debug screenshots (P key)
/dino_screenshot.bmp

# Code::Blocks outputs and IDE state
bin/
obj/
*.layout
*.depend

.DS_Store
